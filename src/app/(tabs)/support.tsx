import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Support() {
    return (
        <SafeAreaView style={{ flex: 1 }}>
            <Text className="font-bold">Suporte Técnico</Text>
        </SafeAreaView>
    )
}